{{-- Shown when the industry/PI recipe tables aren't populated. SDE import is
     removed for now, so this is informational only — no command to run. The
     live-data features (blueprints, jobs, structures, PI colonies) still work. --}}
<div class="im-sde-notice">
    <h4><i class="fas fa-circle-info mr-2"></i> Recipe data not loaded</h4>
    <p>The production calculator, schematics, invention and reactions need EVE's industry recipe data, which this build doesn't load yet.</p>
    <p class="im-text-muted">Everything else works from data SeAT already syncs — your blueprints, industry jobs, structures, and planetary colonies. The recipe-powered tools will light up once that data is imported in a later update.</p>
</div>
